import { State, NotWrappable } from "./state";
declare type ReconcileOptions = {
    key?: string | null;
    merge?: boolean;
};
export declare function reconcile<T>(value: T | State<T>, options?: ReconcileOptions): (state: T extends NotWrappable ? T : State<T>) => void;
export {};
